# Never commit Vault snapshots (the encrypted secret store) or raw evidence logs.
# The integrity register (backup/backup-register.md) IS committed — hashes only.
*.snap
*.snap.gpg
*.tar.gz.gpg
crit8-evidence.log
*-evidence.log
