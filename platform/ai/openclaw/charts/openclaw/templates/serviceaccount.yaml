apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}
  labels:
    app.kubernetes.io/name: openclaw
    app.kubernetes.io/instance: {{ .Release.Name }}
# Defense in depth with the Pod-level setting: the consumer holds no
# Kubernetes credential and receives no provider RBAC.
automountServiceAccountToken: false
