# OK-95 live validation record — 2026-08-11

No credentials, Secrets, or raw diagnostic payloads are stored in this record.

## Talos (`ok-ai`)

- Kubernetes nodes report Talos v1.9.5.
- `platform-diagnostics/openkubes-platform-agent` and
  `platform-diagnostics-readonly` are deployed.
- The ClusterRole contains only `get`, `list`, and `watch`; Secrets are absent.
- Effective authorization permits pod `get`/`list`/`watch` and denies pod
  `create`/`patch`/`update`/`delete`.
- Effective authorization denies every tested Secret operation.
- The running facade declares:

  ```json
  {
    "cilium_diagnostics": true,
    "host_journal": false,
    "node_shell": false,
    "workload_events": true,
    "workload_logs": true
  }
  ```

- A live `collect_diagnostic_evidence` request for `host_journal` and
  `node_shell` returned both requested entries with `status: unavailable` and
  a non-empty reason. Neither entry was silently omitted.

Result: **pass** for the Talos capability delta and read-only provider identity.

## RKE2 (`ok-infra`)

- Nodes report Ubuntu 24.04 and Kubernetes `v1.35.5+rke2r1`, confirming RKE2.
- Profile A is not installed: namespace, ServiceAccount, ClusterRole, facade,
  and collectors are absent.
- No cluster-wide RBAC was added for this validation. A temporary empty
  namespace created during discovery was removed; no validation resources
  remain.

Result: **live validation open**. Static conformance covers the intended RKE2
contract shape, capability declaration, and RBAC manifest. It does not prove a
nonexistent deployment.

## Replay

```bash
cd platform/ai/platform-diagnostics/profiles/kagent/conformance
make verify
```

After Profile A is installed on RKE2, close the remaining evidence gap by:

1. auditing the deployed ServiceAccount and effective authorization;
2. confirming `host_journal` and `node_shell` are declared available;
3. requesting both evidence types; and
4. verifying both are returned as `status: available` with retrievable URIs and
   the same response schema used on Talos.
