# OK-141 M0a v2 risk-acceptance candidate

Status: **AWAITING EXPLICIT ACCEPTANCE / NO-GO**

This package isolates the security risks introduced by the blocked M0a v2
candidate. It records no acceptance and grants no execution authority.

Acceptance of this package would acknowledge only the documented security
model. It would not authorize credential issuance, admission bootstrap, CAAPH
installation, retry, rollback, publication, target convergence, GO-1, or
failure injection. A new executable candidate and new distinct grants would
still be required.
